<form method="post" action="{{ path('ap_role_delete', {'id': ap_role.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_role.id) }}">
    <button class="btn">Delete</button>
</form>
