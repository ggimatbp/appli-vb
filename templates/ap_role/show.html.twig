{% extends 'base.html.twig' %}

{% block title %}ApRole{% endblock %}

{% block body %}
    <h1>ApRole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ap_role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ap_role.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ap_role_index') }}">back to list</a>

    <a href="{{ path('ap_role_edit', {'id': ap_role.id}) }}">edit</a>

    {{ include('ap_role/_delete_form.html.twig') }}
{% endblock %}
