{% extends 'base.html.twig' %}

{% block title %}ApRole{% endblock %}

{% block body %}
    <h1>ApRole</h1>
{{dump()}}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ap_role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ap_role.name }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
        {% for apAccess in ap_role.apaccesses %}
            <tr>
                <th>nom</th>
                <td>{{ apAccess.tab.name }}</td>
            </tr>
            <tr>
                <th></th>
            </tr>
            <tr>
                <th>_view</th>
                <td>{{ apAccess.view ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>_add</th>
                <td>{{ apAccess.add ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>_edit</th>
                <td>{{ apAccess.edit ? 'Yes' : 'No' }}</td>

            </tr>
            <tr>
                <th>delete</th>
                <td>{{ apAccess.delete ? 'Yes' : 'No' }}</td>

            </tr>
            
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('ap_role_index') }}">back to list</a>

    <a href="{{ path('ap_role_edit', {'id': ap_role.id}) }}">edit</a>

    {{ include('ap_role/_delete_form.html.twig') }}
{% endblock %}
