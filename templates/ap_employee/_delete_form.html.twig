<form method="post" action="{{ path('ap_employee_delete', {'id': ap_employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_employee.id) }}">
    <button class="btn">Delete</button>
</form>
