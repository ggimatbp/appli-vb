<form method="post" action="{{ path('app_ap_information_signature_delete', {'id': ap_information_signature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_information_signature.id) }}">
    <button class="btn">Delete</button>
</form>
