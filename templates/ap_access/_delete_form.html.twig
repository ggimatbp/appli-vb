<form method="post" action="{{ path('ap_access_delete', {'id': ap_access.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_access.id) }}">
    <button class="btn">Delete</button>
</form>
