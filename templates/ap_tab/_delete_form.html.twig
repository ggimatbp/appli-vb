<form method="post" action="{{ path('ap_tab_delete', {'id': ap_tab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_tab.id) }}">
    <button class="btn">Delete</button>
</form>
