{% extends 'base.html.twig' %}

{% block title %}ApTab{% endblock %}

{% block body %}
    <h1>ApTab</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ap_tab.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ap_tab.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ap_tab_index') }}">back to list</a>

    <a href="{{ path('ap_tab_edit', {'id': ap_tab.id}) }}">edit</a>

    {{ include('ap_tab/_delete_form.html.twig') }}
{% endblock %}
