{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="aprole-global-container">
        <h1>Utilisateur</h1>
        <div class="aprole-global-container">
            <h4>{{user.firstname}} {{user.lastname}}</h4>
            <table class="table table-hover">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ user.id }}</td>
                    </tr>
                    <tr>
                        <th>Mail</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Roles</th>
                        <td>{{user.roleId.name}}</td>
                    </tr>
                    <tr>
                        <th>Prénom</th>
                        <td>{{ user.firstname }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ user.lastname }}</td>
                    </tr>
                    <tr>
                        <th>Compte actif</th>
                        <td>{{ user.active ? 'Oui' : 'Non' }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="d-flex justify-content-around">
                <a class="btn-bottom btn-edit-role btn btn-ligth" href="{{ path('manager_index') }}">Retour</a>
                <a class="btn-bottom btn-edit-role btn btn-ligth" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
