

				{% if app.user.RoleId is defined %}
						{{ app.user.firstname }}
						{{ app.user.lastname }}
					{% for apAccess in app.user.RoleId.apAccesses %}
						{% if apAccess.view == true %}
							{% if apAccess.tab.apTab is null %}
									<a class="nav-link" aria-current="page" href="{{path (apAccess.tab.path)}}">{{apAccess.tab.name}}</a>								
							{% endif %}
						{% endif %}

					{% endfor %}
          {% set foo = 0 %}
          <ul class="nav nav-pills d-flex justify-content-around" id="pills-tab" role="tablist">
            {% for apAccess in app.user.RoleId.apAccesses %}
              {% if apAccess.view == true %}
              
                {% if apAccess.tab.apTab is not null and app.request.attributes.get('_route') == apAccess.tab.apTab.path %}
                {% set foo = foo + 1 %}
                <li class="nav-item " role="presentation">
                            <button class="nav-link {{foo == 1 ? 'active' : '' }}" id="pills-{{ apAccess.tab.name }}-tab" data-bs-toggle="pill" data-bs-target="#pills-{{ apAccess.tab.name }}" type="button" role="tab" aria-controls="pills-{{ apAccess.tab.name }}" aria-selected="true">{{ apAccess.tab.name }}</button>
                          </li>
                {% endif %}
              {% endif %}
            {% endfor %}
          </ul>
          {% set faa = 0 %}
          <div class="tab-content" id="pills-tabContent">
            {% for apAccess in app.user.RoleId.apAccesses %}
              {% if apAccess.view == true %}
                {% if apAccess.tab.apTab is not null and app.request.attributes.get('_route') == apAccess.tab.apTab.path %}
                {% set faa = faa + 1 %}
                  {{ dump(apAccess) }}
                        <div class="tab-pane fade {{faa == 1 ? 'show active' : '' }}" id="pills-{{ apAccess.tab.name }}" role="tabpanel" aria-labelledby="pills-{{ apAccess.tab.name }}">{% include apAccess.tab.path %}</div>
                {% endif %}
              {% endif %}
            {% endfor %}
          </div>
				{% endif %}


