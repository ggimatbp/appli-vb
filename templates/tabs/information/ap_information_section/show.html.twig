{% extends 'base.html.twig' %}

{% block title %}ApInformationSection{% endblock %}

{% block body %}
    <h1>ApInformationSection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ap_information_section.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ap_information_section.name }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ ap_information_section.state }}</td>
            </tr>
            <tr>
                <th>Archive</th>
                <td>{{ ap_information_section.archive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('information_section_index') }}">back to list</a>

    <a href="{{ path('information_section_edit', {'id': ap_information_section.id}) }}">edit</a>

    {{ include('ap_information_section/_delete_form.html.twig') }}
{% endblock %}
