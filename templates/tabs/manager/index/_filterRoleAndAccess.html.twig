        
<form>
    <tr>
        <th> 
        <input type="text" placeholder="Nom" id="filter-role-lastname-input" class="form-control form-filter-input-global" value="{{ (roleFilterSession is empty ) ? '' : roleFilterSession['ajaxFilterRoleName'] }}">
        </th>
        <th class="text-end"><button id="btn-role-search-filter" class="btn-search"><i class="search-image fas fa-search"></i></button>
        <button id="btn-role-erease-filter" class="btn-search"><i class="search-image fas fa-eraser"></i></button></th>
    </tr>
    <input  id="hidden-role-input-order-and-name" type="hidden" data-ajaxLimitRole="{{(roleFilterSession is empty ) ? limitRole : roleFilterSession['limitRole'] }}" data-ajaxOrderRole="{{ (roleFilterSession is empty ) ? '3' : roleFilterSession['ajaxRoleOrder'] }}" data-ajaxRoleFilterNameOrder="">
</form>