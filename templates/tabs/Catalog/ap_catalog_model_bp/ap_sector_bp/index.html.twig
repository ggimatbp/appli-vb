{% extends 'base.html.twig' %}

{% block title %}ApSectorBp index{% endblock %}

{% block body %}
    <h1>ApSectorBp index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ap_sector_bp in ap_sector_bps %}
            <tr>
                <td>{{ ap_sector_bp.id }}</td>
                <td>{{ ap_sector_bp.name }}</td>
                <td>
                    <a href="{{ path('ap_sector_bp_show', {'id': ap_sector_bp.id}) }}">show</a>
                    <a href="{{ path('ap_sector_bp_edit', {'id': ap_sector_bp.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ap_sector_bp_new') }}">Create new</a>
{% endblock %}
