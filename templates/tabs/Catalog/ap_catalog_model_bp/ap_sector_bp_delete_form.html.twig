<form method="post" action="{{ path('ap_sector_bp_delete', {'id': ap_sector_bp.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_sector_bp.id) }}">
    <button class="btn">Supprimer</button>
</form>
