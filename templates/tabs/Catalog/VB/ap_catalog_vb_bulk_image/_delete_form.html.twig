<form method="post" action="{{ path('catalog_vb_bulk_image_delete', {'id': ap_catalog_vb_bulk_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ap_catalog_vb_bulk_image.id) }}">
    <button class="btn btn-delete">Supprimer</button>
</form>
