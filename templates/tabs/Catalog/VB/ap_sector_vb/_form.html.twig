{{ form_start(form, {'attr': {'onsubmit': 'buttonName.disabled=true; return true;'}}) }}
<div class="form-div-edit-single p-5">
    {{ form_widget(form.name,{'attr': {'class': 'form-new-single form-new-phone-size'}}) }}
    <button class="form-new-phone-size btn btn-ligth" name="buttonName" value="Submit">{{ button_label|default('Envoyer') }}</button>
</div>
{{ form_end(form) }}

{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }} #}


{# {{ form_start(form, {'attr': {'onsubmit': 'buttonName.disabled=true; return true;'}}) }}
<div class="form-div-edit-single p-5">
    {{ form_widget(form.name,{'attr': {'class': 'form-new-single form-new-phone-size'}}) }}
    <button class="form-new-phone-size btn btn-ligth" name="buttonName" value="Submit">{{ button_label|default('Envoyer') }}</button>
</div>
{{ form_end(form) }} #}