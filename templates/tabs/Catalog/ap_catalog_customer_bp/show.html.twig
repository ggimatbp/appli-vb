{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}
    <h1>Client</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ap_catalog_customer_bp.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ap_catalog_customer_bp.name }}</td>
            </tr>
        </tbody>
    </table>
    <ul>
    {% for model in ModelById %}
        <li>{{model.name}}</li>
        <a href="{{ path('ap_catalog_model_bp_show', {'id': model.id}) }}">show</a>
        <a href="{{ path('ap_catalog_model_bp_edit', {'id': model.id}) }}">edit</a>
    {% else %}
            <li><p>Aucun résultat</p></li>
    {% endfor %}
    </ul>
    <a href="{{ path('ap_catalog_customer_bp_index') }}">back to list</a>
    <a href="{{ path('ap_catalog_customer_bp_edit', {'id': ap_catalog_customer_bp.id}) }}">edit</a>
    {{ include('tabs/Catalog/ap_catalog_customer_bp/_delete_form.html.twig') }}
{% endblock %}
