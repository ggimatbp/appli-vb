{% extends 'base.html.twig' %}

{% block title %}ApInformationParapher index{% endblock %}

{% block body %}
    <h1>ApInformationParapher index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateTime</th>
                <th>State</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ap_information_parapher in ap_information_paraphers %}
            <tr>
                <td>{{ ap_information_parapher.id }}</td>
                <td>{{ ap_information_parapher.dateTime ? ap_information_parapher.dateTime|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ ap_information_parapher.state ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_ap_information_parapher_show', {'id': ap_information_parapher.id}) }}">show</a>
                    <a href="{{ path('app_ap_information_parapher_edit', {'id': ap_information_parapher.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ap_information_parapher_new') }}">Create new</a>
{% endblock %}
